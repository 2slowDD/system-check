# sc-sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add safe `sc-sync` behavior to the shared `system-check` skill.

**Architecture:** Implement the command as skill instructions, not as a separate executable, so Codex and Claude both inherit the same behavior. Reuse the existing host-specific sync scripts after a safe Git fast-forward.

**Tech Stack:** Markdown skill instructions, Node.js release verification, GitHub Actions CI.

---

### Task 1: Verification

**Files:**
- Create: `scripts/verify-release.js`

- [x] Write a verification script that asserts the README, changelog, and skill instructions include `0.1.5` and `sc-sync`.
- [x] Run `node scripts/verify-release.js` before implementation and confirm it fails because the new release text is missing.

### Task 2: Skill Behavior

**Files:**
- Modify: `skill/SKILL.md`

- [x] Add `sc-sync` as a built-in command handled before partial target suggestions.
- [x] Document repository resolution, fast-forward-only update behavior, Codex sync scripts, Claude sync scripts, and hard stop conditions.

### Task 3: Release Docs

**Files:**
- Modify: `README.md`
- Modify: `CHANGELOG.md`
- Modify: `.github/workflows/ci.yml`

- [x] Bump README badge to `0.1.5`.
- [x] Add `sc-sync` usage and safety notes to README.
- [x] Add changelog entry for `0.1.5`.
- [x] Add release verification to CI.

### Task 4: Verify

**Files:**
- Verify all changed files.

- [x] Run `node scripts/verify-release.js`.
- [x] Run PowerShell syntax checks.
- [x] Run shell syntax checks.
